/**
 * MCP server is planned for v0.4 (docs/08, docs/09).
 * This package is a placeholder so the monorepo layout matches docs/02.
 */
export function mcpNotImplemented(): never {
  throw new Error(
    "next-architect MCP server is planned for v0.4. Use the CLI (`next-architect check`) for now.",
  );
}
